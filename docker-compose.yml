services:
  backend:
    build: ./Backend
    container_name: erms-backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env

  frontend:
    build: ./Frontend
    container_name: erms-frontend
    ports:
      - "5173:5173"
    command: npm run dev -- --host